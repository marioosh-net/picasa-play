# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                           controllers.Application.albumsPartial()
GET     /album/:serviceIndex/:albumId               controllers.Application.photos(serviceIndex:Int, albumId)
GET     /auth                                       controllers.Application.auth()
GET     /token                                      controllers.Application.token(token)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                               controllers.Assets.at(path="/public", file)
